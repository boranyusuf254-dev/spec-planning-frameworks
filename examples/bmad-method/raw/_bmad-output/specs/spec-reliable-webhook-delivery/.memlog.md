---
topic: Reliable webhook delivery for a TypeScript SaaS API
updated: 2026-07-19T13:28
---

- (direction) Headless, express mode per caller instruction; brief.md is the complete product input, no added product requirements; every unspecified choice recorded as assumption or open question.
- (note) Source: brief.md, Case ID reliable-webhook-delivery (matches slug); scope is planning artifacts only, not implementation.
- (capability) CAP-1 Signed webhooks — intent: recipients can verify a webhook payload's authenticity and integrity via a platform-provided signature; success: a receiver can confirm a genuine payload verifies and a tampered or unsigned payload fails verification.
- (capability) CAP-2 Idempotency — intent: recipients can safely process a delivery attempt received more than once without duplicating side effects; success: two delivery attempts carrying the same idempotency identifier for one event are recognizable by the recipient as duplicates.
- (capability) CAP-3 Retries with exponential backoff and jitter — intent: a failed delivery attempt is retried automatically on a schedule that grows exponentially and includes randomized jitter; success: the recorded attempt timestamps for a failing event show increasing, jittered intervals between attempts.
- (capability) CAP-4 Dead-letter handling — intent: an event that exhausts its retry attempts moves to a dead-letter state instead of being silently dropped; success: an event that fails past the retry limit is present and retrievable in a dead-letter store.
- (capability) CAP-5 Manual replay — intent: an operator can manually resubmit a dead-lettered or previously delivered event for a fresh delivery attempt; success: triggering replay on an event produces a new delivery attempt against the tenant's current endpoint configuration, recorded as such.
- (capability) CAP-6 Delivery audit log — intent: every delivery attempt for every event is recorded for later inspection; success: querying the audit log for a given event returns its complete attempt history (timestamps, outcomes).
- (capability) CAP-7 Tenant isolation — intent: one tenant's webhook configuration, events, and delivery/audit data are inaccessible to and unaffected by another tenant; success: an action or query scoped to tenant A cannot read or modify data belonging to tenant B.
- (capability) CAP-8 Observability — intent: operators can monitor the health of webhook delivery across tenants; success: delivery success rate, latency, and failure counts are observable for a given time window.
- (capability) CAP-9 Acceptance test coverage — intent: the design defines an acceptance test suite that verifies CAP-1 through CAP-8; success: an acceptance test exists and passes for each of CAP-1 through CAP-8.
- (constraint) Delivery mechanism must be implemented in TypeScript (brief names 'TypeScript SaaS API'); rules out non-TS runtime/language choices for the delivery service.
- (decision) Non-goal: implementation and infrastructure provisioning for the webhook delivery service are out of scope — comparison boundary in brief.md states this covers planning artifacts, not an implementation.
- (decision) Non-goal: inbound webhook receipt/ingestion (this SaaS API acting as a receiver of third-party webhooks) is out of scope — brief only describes outbound delivery to tenant-configured endpoints.
- (decision) Success signal: every emitted event reaches its tenant's configured endpoint (verified via signature, deduplicated via idempotency key) within the retry policy's window, or lands in a recoverable dead-letter store; every attempt across success, failure, retry, dead-letter, and replay is visible in a per-tenant audit trail; and the CAP-1..CAP-9 acceptance suite passes.
- (assumption) Assumed 'delivery' means an outbound HTTP callback to a tenant-configured webhook endpoint URL (standard webhook semantics); brief does not define the transport.
- (assumption) Assumed 'signed webhooks' means the platform signs outgoing payloads for the receiving tenant endpoint to verify, the standard outbound-webhook signing direction.
- (assumption) Assumed 'delivery audit log' (CAP-6) must capture per-attempt records, not just final event status, since a log that only shows the last outcome would not satisfy 'audit'.
- (assumption) Assumed 'acceptance tests' (CAP-9) refers to a test suite validating CAP-1..CAP-8 as part of this design's own deliverables, not tenant-facing or end-user tests.
- (question) Which message queue / broker technology backs retry scheduling and dead-lettering? Deliberately unspecified in brief.md.
- (question) Which datastore holds the audit log and dead-letter store? Deliberately unspecified in brief.md.
- (question) Which cloud provider (or on-prem) hosts the service? Deliberately unspecified in brief.md.
- (question) Which HTTP framework implements the delivery service and any admin endpoints? Deliberately unspecified in brief.md.
- (question) Which signature algorithm (e.g., HMAC-SHA256 vs asymmetric signing) is used for CAP-1? Deliberately unspecified in brief.md.
- (question) What is the retry limit (max attempt count) before an event moves to dead-letter (CAP-3/CAP-4)? Deliberately unspecified in brief.md.
- (question) What is the retention period for the audit log and dead-letter store (CAP-4/CAP-6)? Deliberately unspecified in brief.md.
- (question) Is a user interface provided for manual replay, audit log review, or observability (CAP-5/CAP-6/CAP-8), or is this API/CLI only? Deliberately unspecified in brief.md.
- (question) Does the design need to guarantee per-tenant delivery ordering, or is unordered at-least-once delivery acceptable? Not addressed by brief.md; recognized domain implication of retries plus idempotency (CAP-2/CAP-3).
- (question) How are tenant-supplied webhook endpoint URLs validated to prevent SSRF (e.g., requests to internal/private network addresses)? Not addressed by brief.md; recognized domain implication of accepting tenant-configured URLs (CAP-7).
- (question) How are signing keys generated, stored, and rotated (CAP-1)? Not addressed by brief.md; recognized domain implication of a signing capability.
- (question) Who is authorized to trigger a manual replay (CAP-5) — platform operators only, or tenant self-service? Not addressed by brief.md; affects access-control design.
- (event) Self-validate pass 1 (coherence, rules 1-6,8): PASS. All 9 capabilities carry intent+success; intents are WHAT not HOW; the sole constraint (TypeScript) bends a real design decision; 2 non-goals present; success signal is concrete/testable; capability IDs unique and stable; prose is lean.
- (event) Self-validate pass 2 (preservation): PASS. Every brief.md claim (9 coverage items, TypeScript platform, comparison-boundary scope note, all 8 deliberately-unspecified items) lands in SPEC.md as a capability, constraint, non-goal, or open question. No companions needed — no diagrams, tables, or catalogs in source. Process/meta framing (comparison-exercise instructions) intentionally excluded as wrapper, not product content.
- (event) Spec finalized: SPEC.md rendered from memlog, 9 capabilities, 1 constraint, 2 non-goals, 1 success signal, 4 assumptions, 12 open questions, 0 companions.
